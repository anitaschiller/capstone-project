The NewGroup input field unfolds onClick. In combination with a form, the plus icon's position is planned to be fixed on the right side (can be seen if `isStatic` is removed in the code below).

```js
<NewGroup isStatic />
```
